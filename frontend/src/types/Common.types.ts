export type Id = string
export type Timestamp = number

export const UNKNOWN_ID = "unknown"

export enum SUPPORTED_SYNTAX_LANGUAGES {
  SQL = "sql",
  JSX = "jsx",
  JSON = "json",
  CLINGOV_URL = "clingov_url",
  YAML = "yaml",
  PLOTLY = "plotly",
  IMAGEURL = "ImageURL",
}
